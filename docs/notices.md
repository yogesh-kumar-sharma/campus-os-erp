# Notice Module

Administrators create, update, and delete notices. Set an audience to `admin`,
`faculty`, `student`, or leave it open to all. Authenticated users read their
filtered feed at `GET /api/v1/notices/me`.
